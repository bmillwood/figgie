(executables (
  (names (main))
  (libraries (
    figgie

    async_js
    async_kernel
    async_rpc_kernel
    core_kernel
    js_of_ocaml
    incr_dom
  ))
  (js_of_ocaml (
  ))
  (preprocess (pps (
    ppx_jane
    js_of_ocaml-ppx
  )))
))
